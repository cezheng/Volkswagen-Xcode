machine:
  xcode:
    version: "7.0"
test:
  override:
    - xcodebuild -workspace Volkswagen.xcworkspace -scheme Volkswagen-iOS -sdk iphonesimulator -destination 'platform=iOS Simulator,name=iPhone 6,OS=latest' test

      